package structural_patterns.adapter;

public class IncomingModel {
    public void displayData(JsonData jsonData) {
        // Make use of the JsonData to display data
    }
    public void displayStats(JsonData jsonData) {
        // Make use of the JsonData to display data
    }
}
